% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/status_geopressuretemplate.R
\name{status_geopressuretemplate}
\alias{status_geopressuretemplate}
\title{Inspect GeoPressureTemplate files}
\usage{
status_geopressuretemplate(directory = ".", read_interim = TRUE)
}
\arguments{
\item{directory}{Path to the GeoPressureTemplate directory.}

\item{read_interim}{Logical. If \code{TRUE}, read interim \code{.RData} files and add
columns for selected saved objects and tag status. If \code{FALSE}, only file
discovery is performed.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per tag and columns:
\itemize{
\item \code{tag_id}: tag identifier;
\item \code{in_config}, \code{in_interim}, \code{in_raw_tag}: whether the tag was found in each
project source;
\item when \code{read_interim = TRUE}, \verb{interim_*} columns indicating which workflow
objects are saved in the interim file, plus \code{tag_status} from
\code{\link[GeoPressureR:tag_status]{GeoPressureR::tag_status()}} when a saved \code{tag} object is available.
}
}
\description{
Summarize which tags are discoverable in a GeoPressureTemplate project from
\code{config.yml}, \code{data/interim}, and \code{data/raw-tag}. Use this before importing
a project to see which tags are available from each source and, optionally,
which workflow objects are present in interim files.

During automatic discovery, names starting with \code{"_"} are treated as private
project entries and skipped. This applies to config keys, interim files,
raw-tag folders, and files inside raw-tag folders. Empty raw-tag folders are
also skipped, so \code{in_raw_tag = TRUE} means the folder contains at least one
non-ignored file.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\examples{
\dontrun{
status_geopressuretemplate()
status_geopressuretemplate(read_interim = FALSE)
}

}
